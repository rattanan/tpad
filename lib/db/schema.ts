import {
  boolean,
  datetime,
  index,
  int,
  longtext,
  mysqlEnum,
  mysqlTable,
  primaryKey,
  text,
  uniqueIndex,
  varchar,
} from "drizzle-orm/mysql-core";

export const roleValues = ["ADMIN", "DATA_SOURCE_CREATOR", "DASHBOARD_CREATOR", "VIEWER"] as const;
export const statusValues = ["ACTIVE", "INACTIVE", "LOCKED", "ARCHIVED"] as const;
export type Role = (typeof roleValues)[number];
export type UserStatus = (typeof statusValues)[number];

export const users = mysqlTable("users", {
  id: varchar("id", { length: 36 }).primaryKey(),
  fullName: varchar("full_name", { length: 160 }).notNull(),
  username: varchar("username", { length: 80 }).notNull(),
  email: varchar("email", { length: 190 }).notNull(),
  passwordHash: varchar("password_hash", { length: 255 }).notNull(),
  role: mysqlEnum("role", roleValues).notNull().default("VIEWER"),
  status: mysqlEnum("status", statusValues).notNull().default("ACTIVE"),
  adminNotes: text("admin_notes"),
  failedLoginAttempts: int("failed_login_attempts").notNull().default(0),
  failedLoginWindowStartedAt: datetime("failed_login_window_started_at", { mode: "date", fsp: 3 }),
  lockedUntil: datetime("locked_until", { mode: "date", fsp: 3 }),
  mustChangePassword: boolean("must_change_password").notNull().default(true),
  lastLoginAt: datetime("last_login_at", { mode: "date", fsp: 3 }),
  passwordChangedAt: datetime("password_changed_at", { mode: "date", fsp: 3 }),
  createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
  updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
  createdBy: varchar("created_by", { length: 36 }),
  updatedBy: varchar("updated_by", { length: 36 }),
  archivedAt: datetime("archived_at", { mode: "date", fsp: 3 }),
}, (table) => [
  uniqueIndex("users_email_uq").on(table.email),
  uniqueIndex("users_username_uq").on(table.username),
  index("users_status_idx").on(table.status),
  index("users_role_idx").on(table.role),
]);

export const sessions = mysqlTable("sessions", {
  id: varchar("id", { length: 36 }).primaryKey(),
  userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }),
  sessionTokenHash: varchar("session_token_hash", { length: 64 }).notNull(),
  ipAddress: varchar("ip_address", { length: 64 }),
  userAgent: text("user_agent"),
  lastActiveAt: datetime("last_active_at", { mode: "date", fsp: 3 }).notNull(),
  expiresAt: datetime("expires_at", { mode: "date", fsp: 3 }).notNull(),
  revokedAt: datetime("revoked_at", { mode: "date", fsp: 3 }),
  createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (table) => [
  uniqueIndex("sessions_token_uq").on(table.sessionTokenHash),
  index("sessions_user_idx").on(table.userId),
  index("sessions_expires_idx").on(table.expiresAt),
]);

export const loginHistory = mysqlTable("login_history", {
  id: varchar("id", { length: 36 }).primaryKey(),
  userId: varchar("user_id", { length: 36 }).references(() => users.id, { onDelete: "set null" }),
  loginIdentifier: varchar("login_identifier", { length: 190 }).notNull(),
  eventType: mysqlEnum("event_type", ["LOGIN", "LOGOUT", "SESSION"]).notNull(),
  status: mysqlEnum("status", ["SUCCESS", "FAILED", "LOCKED", "LOGOUT", "SESSION_EXPIRED"]).notNull(),
  ipAddress: varchar("ip_address", { length: 64 }),
  userAgent: text("user_agent"),
  browser: varchar("browser", { length: 80 }),
  operatingSystem: varchar("operating_system", { length: 80 }),
  deviceType: varchar("device_type", { length: 40 }),
  failureReason: varchar("failure_reason", { length: 160 }),
  loggedInAt: datetime("logged_in_at", { mode: "date", fsp: 3 }),
  loggedOutAt: datetime("logged_out_at", { mode: "date", fsp: 3 }),
  createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (table) => [
  index("login_history_user_idx").on(table.userId),
  index("login_history_created_idx").on(table.createdAt),
  index("login_history_status_idx").on(table.status),
]);

export const auditLogs = mysqlTable("audit_logs", {
  id: varchar("id", { length: 36 }).primaryKey(),
  actorUserId: varchar("actor_user_id", { length: 36 }).references(() => users.id, { onDelete: "set null" }),
  actorName: varchar("actor_name", { length: 160 }),
  action: varchar("action", { length: 100 }).notNull(),
  category: varchar("category", { length: 60 }).notNull(),
  targetType: varchar("target_type", { length: 60 }),
  targetId: varchar("target_id", { length: 80 }),
  targetName: varchar("target_name", { length: 190 }),
  result: mysqlEnum("result", ["SUCCESS", "FAILED"]).notNull(),
  description: text("description"),
  previousValues: longtext("previous_values"),
  newValues: longtext("new_values"),
  ipAddress: varchar("ip_address", { length: 64 }),
  userAgent: text("user_agent"),
  requestId: varchar("request_id", { length: 36 }).notNull(),
  createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (table) => [
  index("audit_actor_idx").on(table.actorUserId),
  index("audit_action_idx").on(table.action),
  index("audit_target_idx").on(table.targetType, table.targetId),
  index("audit_created_idx").on(table.createdAt),
]);

export const passwordHistory = mysqlTable("password_history", {
  id: varchar("id", { length: 36 }).primaryKey(),
  userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }),
  passwordHash: varchar("password_hash", { length: 255 }).notNull(),
  createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (table) => [index("password_history_user_idx").on(table.userId, table.createdAt)]);

export const loginRateLimits = mysqlTable("login_rate_limits", {
  ipHash: varchar("ip_hash", { length: 64 }).notNull(),
  windowStartedAt: datetime("window_started_at", { mode: "date", fsp: 3 }).notNull(),
  attempts: int("attempts").notNull().default(0),
  blockedUntil: datetime("blocked_until", { mode: "date", fsp: 3 }),
  updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (table) => [primaryKey({ columns: [table.ipHash] })]);

export const dataSourceStatusValues = ["DRAFT", "ACTIVE", "INACTIVE", "CONNECTION_ERROR", "ARCHIVED"] as const;
export const connectionStatusValues = ["NOT_TESTED", "CONNECTED", "FAILED", "TIMEOUT"] as const;
export const metadataSyncStatusValues = ["NOT_SYNCED", "SYNCING", "SYNCED", "PARTIAL", "FAILED"] as const;
export const environmentValues = ["DEVELOPMENT", "TEST", "UAT", "PRODUCTION"] as const;
export const accessPermissionValues = ["VIEW_METADATA", "PREVIEW_DATA", "USE_FOR_DASHBOARD", "EDIT_METADATA", "MANAGE_CONNECTION", "SYNC_METADATA", "MANAGE_BUSINESS_CONTEXT", "PUBLISH_BUSINESS_CONTEXT", "USE_BUSINESS_CONTEXT"] as const;
export type DataSourceAccessPermission = (typeof accessPermissionValues)[number];

export const dataSources = mysqlTable("data_sources", {
  id: varchar("id", { length: 36 }).primaryKey(), name: varchar("name", { length: 160 }).notNull(), description: text("description"),
  databaseType: varchar("database_type", { length: 40 }).notNull().default("ORACLE"), environment: mysqlEnum("environment", environmentValues).notNull(),
  status: mysqlEnum("status", dataSourceStatusValues).notNull().default("DRAFT"), host: varchar("host", { length: 255 }).notNull(), port: int("port").notNull().default(1521),
  connectionMode: mysqlEnum("connection_mode", ["SERVICE_NAME", "SID", "CONNECTION_STRING"]).notNull().default("SERVICE_NAME"), serviceName: varchar("service_name", { length: 255 }), sid: varchar("sid", { length: 255 }), connectionString: text("connection_string"),
  username: varchar("username", { length: 255 }).notNull(), encryptedPassword: longtext("encrypted_password").notNull(), passwordIv: varchar("password_iv", { length: 64 }).notNull(), passwordAuthTag: varchar("password_auth_tag", { length: 64 }).notNull(), passwordKeyVersion: varchar("password_key_version", { length: 40 }).notNull().default("v1"),
  defaultSchema: varchar("default_schema", { length: 128 }), allowedSchemas: longtext("allowed_schemas"), connectionTimeoutSeconds: int("connection_timeout_seconds").notNull().default(10), queryTimeoutSeconds: int("query_timeout_seconds").notNull().default(30),
  connectionStatus: mysqlEnum("connection_status", connectionStatusValues).notNull().default("NOT_TESTED"), metadataSyncStatus: mysqlEnum("metadata_sync_status", metadataSyncStatusValues).notNull().default("NOT_SYNCED"),
  lastConnectionTestAt: datetime("last_connection_test_at", { mode: "date", fsp: 3 }), lastSuccessfulConnectionAt: datetime("last_successful_connection_at", { mode: "date", fsp: 3 }), lastMetadataSyncAt: datetime("last_metadata_sync_at", { mode: "date", fsp: 3 }), databaseVersion: varchar("database_version", { length: 255 }),
  ownerUserId: varchar("owner_user_id", { length: 36 }).notNull().references(() => users.id), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), archivedAt: datetime("archived_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("data_sources_name_environment_uq").on(t.name, t.environment), index("data_sources_status_idx").on(t.status), index("data_sources_owner_idx").on(t.ownerUserId)]);

export const dataSourceSchemas = mysqlTable("data_source_schemas", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), schemaName: varchar("schema_name", { length: 128 }).notNull(), tableCount: int("table_count").notNull().default(0), viewCount: int("view_count").notNull().default(0), isIncluded: boolean("is_included").notNull().default(false), status: mysqlEnum("status", ["ACTIVE", "MISSING", "EXCLUDED", "ARCHIVED"]).notNull().default("ACTIVE"), lastSyncedAt: datetime("last_synced_at", { mode: "date", fsp: 3 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("data_source_schemas_uq").on(t.dataSourceId, t.schemaName)]);

export const dataSourceTables = mysqlTable("data_source_tables", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), schemaId: varchar("schema_id", { length: 36 }).references(() => dataSourceSchemas.id, { onDelete: "set null" }), schemaName: varchar("schema_name", { length: 128 }).notNull(), tableName: varchar("table_name", { length: 128 }).notNull(), businessName: varchar("business_name", { length: 255 }), objectType: mysqlEnum("object_type", ["TABLE", "VIEW"]).notNull(), description: text("description"), estimatedRowCount: int("estimated_row_count"), lastAnalyzedAt: datetime("last_analyzed_at", { mode: "date", fsp: 3 }), isPartitioned: boolean("is_partitioned").notNull().default(false), isTemporary: boolean("is_temporary").notNull().default(false), isIncluded: boolean("is_included").notNull().default(true), status: mysqlEnum("status", ["ACTIVE", "MISSING", "EXCLUDED", "ARCHIVED"]).notNull().default("ACTIVE"), metadataHash: varchar("metadata_hash", { length: 64 }), lastSyncedAt: datetime("last_synced_at", { mode: "date", fsp: 3 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("data_source_tables_uq").on(t.dataSourceId, t.schemaName, t.tableName), index("data_source_tables_source_idx").on(t.dataSourceId)]);

export const dataSourceColumns = mysqlTable("data_source_columns", {
  id: varchar("id", { length: 36 }).primaryKey(), tableId: varchar("table_id", { length: 36 }).notNull().references(() => dataSourceTables.id, { onDelete: "cascade" }), columnName: varchar("column_name", { length: 128 }).notNull(), businessName: varchar("business_name", { length: 255 }), description: text("description"), dataType: varchar("data_type", { length: 128 }).notNull(), dataLength: int("data_length"), numericPrecision: int("numeric_precision"), numericScale: int("numeric_scale"), nullable: boolean("nullable").notNull().default(true), defaultValue: text("default_value"), ordinalPosition: int("ordinal_position").notNull(), isPrimaryKey: boolean("is_primary_key").notNull().default(false), isForeignKey: boolean("is_foreign_key").notNull().default(false), isIncluded: boolean("is_included").notNull().default(true), sensitivityType: mysqlEnum("sensitivity_type", ["NONE", "PERSONAL_DATA", "SENSITIVE_PERSONAL_DATA", "FINANCIAL", "CREDENTIAL", "CONTACT", "IDENTIFIER", "CONFIDENTIAL"]).notNull().default("NONE"), suggestedSensitivityType: mysqlEnum("suggested_sensitivity_type", ["NONE", "PERSONAL_DATA", "SENSITIVE_PERSONAL_DATA", "FINANCIAL", "CREDENTIAL", "CONTACT", "IDENTIFIER", "CONFIDENTIAL"]).notNull().default("NONE"), maskingRule: varchar("masking_rule", { length: 80 }), status: mysqlEnum("status", ["ACTIVE", "MISSING", "EXCLUDED", "ARCHIVED"]).notNull().default("ACTIVE"), metadataHash: varchar("metadata_hash", { length: 64 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("data_source_columns_uq").on(t.tableId, t.columnName)]);

export const dataSourceRelationships = mysqlTable("data_source_relationships", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), sourceTableId: varchar("source_table_id", { length: 36 }).notNull().references(() => dataSourceTables.id, { onDelete: "cascade" }), sourceColumnId: varchar("source_column_id", { length: 36 }).references(() => dataSourceColumns.id, { onDelete: "set null" }), targetTableId: varchar("target_table_id", { length: 36 }).notNull().references(() => dataSourceTables.id, { onDelete: "cascade" }), targetColumnId: varchar("target_column_id", { length: 36 }).references(() => dataSourceColumns.id, { onDelete: "set null" }), constraintName: varchar("constraint_name", { length: 128 }), relationshipType: varchar("relationship_type", { length: 40 }).notNull().default("MANY_TO_ONE"), discoveryMethod: mysqlEnum("discovery_method", ["FOREIGN_KEY", "NAME_PATTERN", "MANUAL"]).notNull().default("FOREIGN_KEY"), confidenceScore: int("confidence_score").notNull().default(100), status: mysqlEnum("status", ["SUGGESTED", "CONFIRMED", "REJECTED", "DISABLED"]).notNull().default("CONFIRMED"), reviewedBy: varchar("reviewed_by", { length: 36 }), reviewedAt: datetime("reviewed_at", { mode: "date", fsp: 3 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("relationships_source_idx").on(t.sourceTableId), index("relationships_target_idx").on(t.targetTableId), uniqueIndex("relationships_constraint_uq").on(t.dataSourceId, t.constraintName, t.sourceColumnId)]);

export const dataSourceIndexes = mysqlTable("data_source_indexes", { id: varchar("id", { length: 36 }).primaryKey(), tableId: varchar("table_id", { length: 36 }).notNull().references(() => dataSourceTables.id, { onDelete: "cascade" }), indexName: varchar("index_name", { length: 128 }).notNull(), columnName: varchar("column_name", { length: 128 }).notNull(), columnPosition: int("column_position").notNull(), isUnique: boolean("is_unique").notNull().default(false), status: mysqlEnum("status", ["ACTIVE", "MISSING", "ARCHIVED"]).notNull().default("ACTIVE"), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull() }, (t) => [uniqueIndex("data_source_indexes_uq").on(t.tableId, t.indexName, t.columnName), index("data_source_indexes_table_idx").on(t.tableId)]);

export const metadataSyncRuns = mysqlTable("metadata_sync_runs", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), syncType: mysqlEnum("sync_type", ["FULL", "INCREMENTAL", "SCHEMA", "TABLE"]).notNull().default("FULL"), status: mysqlEnum("status", ["QUEUED", "RUNNING", "COMPLETED", "PARTIAL", "FAILED", "CANCELLED"]).notNull().default("QUEUED"), progressStep: varchar("progress_step", { length: 80 }), startedBy: varchar("started_by", { length: 36 }).notNull(), startedAt: datetime("started_at", { mode: "date", fsp: 3 }).notNull(), completedAt: datetime("completed_at", { mode: "date", fsp: 3 }), durationMs: int("duration_ms"), schemasProcessed: int("schemas_processed").notNull().default(0), tablesFound: int("tables_found").notNull().default(0), viewsFound: int("views_found").notNull().default(0), columnsFound: int("columns_found").notNull().default(0), primaryKeysFound: int("primary_keys_found").notNull().default(0), foreignKeysFound: int("foreign_keys_found").notNull().default(0), relationshipsFound: int("relationships_found").notNull().default(0), newObjects: int("new_objects").notNull().default(0), updatedObjects: int("updated_objects").notNull().default(0), missingObjects: int("missing_objects").notNull().default(0), warningCount: int("warning_count").notNull().default(0), errorCount: int("error_count").notNull().default(0), errorSummary: text("error_summary"), requestId: varchar("request_id", { length: 36 }).notNull(),
}, (t) => [index("sync_runs_source_idx").on(t.dataSourceId), index("sync_runs_started_idx").on(t.startedAt)]);

export const dataSourceAccess = mysqlTable("data_source_access", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), userId: varchar("user_id", { length: 36 }).references(() => users.id, { onDelete: "cascade" }), role: mysqlEnum("role", roleValues), permission: mysqlEnum("permission", accessPermissionValues).notNull(), grantedBy: varchar("granted_by", { length: 36 }).notNull(), grantedAt: datetime("granted_at", { mode: "date", fsp: 3 }).notNull(), revokedAt: datetime("revoked_at", { mode: "date", fsp: 3 }),
}, (t) => [index("data_source_access_source_idx").on(t.dataSourceId), index("data_source_access_user_idx").on(t.userId)]);

export const dataSourceConnectionTests = mysqlTable("data_source_connection_tests", { id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "cascade" }), status: mysqlEnum("status", connectionStatusValues).notNull(), responseTimeMs: int("response_time_ms"), databaseVersion: varchar("database_version", { length: 255 }), currentUser: varchar("current_user", { length: 255 }), currentSchema: varchar("current_schema", { length: 255 }), errorCode: varchar("error_code", { length: 32 }), errorCategory: varchar("error_category", { length: 80 }), errorMessage: varchar("error_message", { length: 255 }), testedBy: varchar("tested_by", { length: 36 }).notNull(), ipAddress: varchar("ip_address", { length: 64 }), requestId: varchar("request_id", { length: 36 }).notNull(), testedAt: datetime("tested_at", { mode: "date", fsp: 3 }).notNull() }, (t) => [index("connection_tests_source_idx").on(t.dataSourceId)]);

export const businessModelStatusValues = ["DRAFT", "AI_ANALYZING", "READY_FOR_REVIEW", "CHANGES_REQUESTED", "APPROVED", "PUBLISHED", "ARCHIVED"] as const;
export const approvalStatusValues = ["DRAFT", "SUGGESTED", "IN_REVIEW", "APPROVED", "REJECTED"] as const;
export const businessDomainValues = ["PROCUREMENT", "INVENTORY", "MAINTENANCE", "FLEET", "FINANCE", "HUMAN_RESOURCE", "VENDOR", "CUSTOMER", "CONTRACT", "WORK_ORDER", "OTHER"] as const;
export const businessObjectTypeValues = ["TRANSACTION", "MASTER_DATA", "REFERENCE_DATA", "SNAPSHOT", "AGGREGATE", "BRIDGE", "VIEW", "UNKNOWN"] as const;
export const businessTypeValues = ["TEXT", "NUMBER", "CURRENCY", "PERCENTAGE", "BOOLEAN", "DATE", "DATETIME", "DURATION", "QUANTITY", "STATUS", "IDENTIFIER", "GEOGRAPHIC", "URL", "EMAIL", "PHONE", "UNKNOWN"] as const;
export const fieldRoleValues = ["DIMENSION", "MEASURE", "IDENTIFIER", "DATE_DIMENSION", "STATUS_DIMENSION", "FOREIGN_KEY", "TECHNICAL_FIELD", "SENSITIVE_FIELD", "IGNORED"] as const;
export const aggregationRuleValues = ["SUM", "AVERAGE", "COUNT", "COUNT_DISTINCT", "MINIMUM", "MAXIMUM", "LATEST", "EARLIEST", "NONE", "CUSTOM"] as const;
export const sensitivityLevelValues = ["PUBLIC", "INTERNAL", "CONFIDENTIAL", "RESTRICTED"] as const;
export const certificationStatusValues = ["UNVERIFIED", "TECHNICALLY_VALIDATED", "BUSINESS_VALIDATED", "CERTIFIED"] as const;
export const validationOutcomeValues = ["PASSED", "PASSED_WITH_WARNING", "FAILED"] as const;

export const businessContextModels = mysqlTable("business_context_models", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), name: varchar("name", { length: 190 }).notNull(), description: text("description"), schemaName: varchar("schema_name", { length: 128 }).notNull(), version: int("version").notNull().default(1), status: mysqlEnum("status", businessModelStatusValues).notNull().default("DRAFT"), submittedAt: datetime("submitted_at", { mode: "date", fsp: 3 }), submittedBy: varchar("submitted_by", { length: 36 }), approvedAt: datetime("approved_at", { mode: "date", fsp: 3 }), approvedBy: varchar("approved_by", { length: 36 }), publishedAt: datetime("published_at", { mode: "date", fsp: 3 }), publishedBy: varchar("published_by", { length: 36 }), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("business_context_models_source_name_uq").on(t.dataSourceId, t.name), index("business_context_models_status_idx").on(t.status), index("business_context_models_source_idx").on(t.dataSourceId)]);

export const businessDomains = mysqlTable("business_domains", {
  id: varchar("id", { length: 36 }).primaryKey(), dataSourceId: varchar("data_source_id", { length: 36 }).references(() => dataSources.id, { onDelete: "restrict" }), modelId: varchar("model_id", { length: 36 }).references(() => businessContextModels.id, { onDelete: "restrict" }), code: varchar("code", { length: 80 }).notNull(), name: varchar("name", { length: 160 }).notNull(), nameTh: varchar("name_th", { length: 160 }), description: text("description"), version: int("version").notNull().default(1), status: mysqlEnum("status", approvalStatusValues).notNull().default("DRAFT"), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("business_domains_model_code_uq").on(t.modelId, t.code), index("business_domains_source_idx").on(t.dataSourceId)]);

export const businessObjects = mysqlTable("business_objects", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), physicalTableId: varchar("physical_table_id", { length: 36 }).notNull().references(() => dataSourceTables.id, { onDelete: "restrict" }), technicalName: varchar("technical_name", { length: 128 }).notNull(), databaseSchema: varchar("database_schema", { length: 128 }).notNull(), businessName: varchar("business_name", { length: 255 }).notNull(), shortName: varchar("short_name", { length: 80 }), description: text("description"), businessDomainId: varchar("business_domain_id", { length: 36 }).references(() => businessDomains.id, { onDelete: "set null" }), objectType: mysqlEnum("object_type", businessObjectTypeValues).notNull().default("UNKNOWN"), primaryKeyDefinition: longtext("primary_key_definition"), defaultDateFieldId: varchar("default_date_field_id", { length: 36 }), recordGrain: varchar("record_grain", { length: 500 }), dataOwner: varchar("data_owner", { length: 190 }), dataSteward: varchar("data_steward", { length: 190 }), tags: longtext("tags"), sensitivityLevel: mysqlEnum("sensitivity_level", sensitivityLevelValues).notNull().default("INTERNAL"), usageStatus: mysqlEnum("usage_status", ["ACTIVE", "DEPRECATED", "HIDDEN"]).notNull().default("ACTIVE"), aiUsageAllowed: boolean("ai_usage_allowed").notNull().default(false), approvalStatus: mysqlEnum("approval_status", approvalStatusValues).notNull().default("DRAFT"), notes: text("notes"), layoutX: int("layout_x").notNull().default(0), layoutY: int("layout_y").notNull().default(0), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("business_objects_model_table_uq").on(t.modelId, t.physicalTableId), index("business_objects_model_idx").on(t.modelId), index("business_objects_approval_idx").on(t.approvalStatus)]);

export const businessFields = mysqlTable("business_fields", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), businessObjectId: varchar("business_object_id", { length: 36 }).notNull().references(() => businessObjects.id, { onDelete: "restrict" }), physicalColumnId: varchar("physical_column_id", { length: 36 }).notNull().references(() => dataSourceColumns.id, { onDelete: "restrict" }), physicalColumnName: varchar("physical_column_name", { length: 128 }).notNull(), businessName: varchar("business_name", { length: 255 }).notNull(), description: text("description"), physicalDataType: varchar("physical_data_type", { length: 128 }).notNull(), businessType: mysqlEnum("business_type", businessTypeValues).notNull().default("UNKNOWN"), fieldRole: mysqlEnum("field_role", fieldRoleValues).notNull().default("DIMENSION"), aggregationRule: mysqlEnum("aggregation_rule", aggregationRuleValues).notNull().default("NONE"), format: varchar("format", { length: 80 }), unit: varchar("unit", { length: 80 }), currency: varchar("currency", { length: 12 }), timeZone: varchar("time_zone", { length: 80 }), nullable: boolean("nullable").notNull().default(true), isUnique: boolean("is_unique").notNull().default(false), isPrimaryKey: boolean("is_primary_key").notNull().default(false), isForeignKey: boolean("is_foreign_key").notNull().default(false), dimensionGroup: varchar("dimension_group", { length: 120 }), exampleValues: longtext("example_values"), sensitivityClassification: mysqlEnum("sensitivity_classification", ["NONE", "PERSONAL_DATA", "SENSITIVE_PERSONAL_DATA", "FINANCIAL", "CREDENTIAL", "CONTACT", "IDENTIFIER", "CONFIDENTIAL"]).notNull().default("NONE"), maskingRule: varchar("masking_rule", { length: 80 }), aiUsageAllowed: boolean("ai_usage_allowed").notNull().default(false), filterable: boolean("filterable").notNull().default(true), groupable: boolean("groupable").notNull().default(true), sortable: boolean("sortable").notNull().default(true), searchable: boolean("searchable").notNull().default(false), visibleToDashboardCreator: boolean("visible_to_dashboard_creator").notNull().default(false), approvalStatus: mysqlEnum("approval_status", approvalStatusValues).notNull().default("DRAFT"), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("business_fields_object_column_uq").on(t.businessObjectId, t.physicalColumnId), index("business_fields_model_idx").on(t.modelId), index("business_fields_object_idx").on(t.businessObjectId)]);

export const businessRelationships = mysqlTable("business_relationships", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), sourceObjectId: varchar("source_object_id", { length: 36 }).notNull().references(() => businessObjects.id, { onDelete: "restrict" }), sourceFieldId: varchar("source_field_id", { length: 36 }).notNull().references(() => businessFields.id, { onDelete: "restrict" }), targetObjectId: varchar("target_object_id", { length: 36 }).notNull().references(() => businessObjects.id, { onDelete: "restrict" }), targetFieldId: varchar("target_field_id", { length: 36 }).notNull().references(() => businessFields.id, { onDelete: "restrict" }), joinType: mysqlEnum("join_type", ["INNER", "LEFT", "RIGHT"]).notNull().default("LEFT"), cardinality: mysqlEnum("cardinality", ["ONE_TO_ONE", "ONE_TO_MANY", "MANY_TO_ONE", "MANY_TO_MANY", "UNKNOWN"]).notNull().default("UNKNOWN"), direction: mysqlEnum("direction", ["BIDIRECTIONAL", "SOURCE_TO_TARGET", "TARGET_TO_SOURCE"]).notNull().default("SOURCE_TO_TARGET"), isRequired: boolean("is_required").notNull().default(false), confidenceScore: int("confidence_score").notNull().default(100), sourceType: mysqlEnum("source_type", ["DATABASE_CONSTRAINT", "AI_SUGGESTED", "MANUAL", "COLUMN_PATTERN"]).notNull().default("MANUAL"), validationStatus: mysqlEnum("validation_status", ["PENDING", "VALID", "WARNING", "INVALID"]).notNull().default("PENDING"), approvalStatus: mysqlEnum("approval_status", approvalStatusValues).notNull().default("DRAFT"), approvedBy: varchar("approved_by", { length: 36 }), approvedAt: datetime("approved_at", { mode: "date", fsp: 3 }), notes: text("notes"), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("business_relationships_model_idx").on(t.modelId), index("business_relationships_source_idx").on(t.sourceObjectId), index("business_relationships_target_idx").on(t.targetObjectId)]);

export const businessRelationshipValidationResults = mysqlTable("business_relationship_validation_results", {
  id: varchar("id", { length: 36 }).primaryKey(), relationshipId: varchar("relationship_id", { length: 36 }).notNull().references(() => businessRelationships.id, { onDelete: "restrict" }), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), result: mysqlEnum("result", validationOutcomeValues).notNull(), ruleCode: varchar("rule_code", { length: 80 }).notNull(), severity: mysqlEnum("severity", ["INFO", "WARNING", "ERROR"]).notNull(), message: text("message").notNull(), suggestedFix: text("suggested_fix"), metrics: longtext("metrics"), validatedBy: varchar("validated_by", { length: 36 }).notNull(), validatedAt: datetime("validated_at", { mode: "date", fsp: 3 }).notNull(), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("relationship_validation_relationship_idx").on(t.relationshipId), index("relationship_validation_model_idx").on(t.modelId)]);

export const kpiStatusValues = ["DRAFT", "UNDER_REVIEW", "CHANGES_REQUESTED", "APPROVED", "CERTIFIED", "DEPRECATED", "ARCHIVED"] as const;
export const kpiDefinitions = mysqlTable("kpi_definitions", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("business_context_model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), businessDomainId: varchar("business_domain_id", { length: 36 }).references(() => businessDomains.id, { onDelete: "set null" }), code: varchar("code", { length: 80 }).notNull(), name: varchar("name", { length: 255 }).notNull(), shortName: varchar("short_name", { length: 120 }), description: text("description"), businessObjective: text("business_objective"), businessQuestion: text("business_question"), owner: varchar("owner", { length: 190 }), dataSteward: varchar("data_steward", { length: 190 }), tags: longtext("tags"), measureType: mysqlEnum("measure_type", ["ADDITIVE", "SEMI_ADDITIVE", "NON_ADDITIVE", "RATIO", "COUNT"]).notNull().default("ADDITIVE"), formulaAst: longtext("formula_ast").notNull(), numeratorDefinition: text("numerator_definition"), denominatorDefinition: text("denominator_definition"), aggregation: varchar("aggregation", { length: 40 }), distinctRule: text("distinct_rule"), nullHandling: mysqlEnum("null_handling", ["ZERO", "IGNORE", "ERROR"]).notNull().default("IGNORE"), divisionByZeroHandling: mysqlEnum("division_by_zero_handling", ["NULL", "ZERO", "ERROR"]).notNull().default("NULL"), decimalPrecision: int("decimal_precision").notNull().default(2), unit: varchar("unit", { length: 80 }), currency: varchar("currency", { length: 12 }), percentageFormat: varchar("percentage_format", { length: 80 }), defaultDateFieldId: varchar("default_date_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "set null" }), dateLogic: longtext("date_logic"), dataFreshnessRequirement: varchar("data_freshness_requirement", { length: 160 }), recommendedVisualization: varchar("recommended_visualization", { length: 80 }), targetDirection: mysqlEnum("target_direction", ["HIGHER_IS_BETTER", "LOWER_IS_BETTER", "TARGET_RANGE"]), targetValue: varchar("target_value", { length: 100 }), goodRange: longtext("good_range"), badRange: longtext("bad_range"), displayFormat: varchar("display_format", { length: 120 }), defaultComparisonPeriod: varchar("default_comparison_period", { length: 80 }), status: mysqlEnum("status", kpiStatusValues).notNull().default("DRAFT"), certificationStatus: mysqlEnum("certification_status", certificationStatusValues).notNull().default("UNVERIFIED"), draftedBy: varchar("drafted_by", { length: 36 }).notNull(), reviewedBy: varchar("reviewed_by", { length: 36 }), approvedBy: varchar("approved_by", { length: 36 }), approvalDate: datetime("approval_date", { mode: "date", fsp: 3 }), version: int("version").notNull().default(1), effectiveDate: datetime("effective_date", { mode: "date", fsp: 3 }), expiryDate: datetime("expiry_date", { mode: "date", fsp: 3 }), changeReason: text("change_reason"), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("kpi_definitions_model_code_uq").on(t.modelId, t.code), index("kpi_definitions_model_idx").on(t.modelId), index("kpi_definitions_status_idx").on(t.status)]);

export const kpiDefinitionVersions = mysqlTable("kpi_definition_versions", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), versionNumber: int("version_number").notNull(), status: mysqlEnum("status", ["APPROVED", "CERTIFIED", "SUPERSEDED"]).notNull(), snapshotJson: longtext("snapshot_json").notNull(), changeReason: text("change_reason"), approvedBy: varchar("approved_by", { length: 36 }), approvedAt: datetime("approved_at", { mode: "date", fsp: 3 }), createdBy: varchar("created_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("kpi_definition_versions_uq").on(t.kpiId, t.versionNumber), index("kpi_definition_versions_kpi_idx").on(t.kpiId)]);

export const kpiFormulaNodes = mysqlTable("kpi_formula_nodes", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), parentNodeId: varchar("parent_node_id", { length: 36 }), nodeType: varchar("node_type", { length: 40 }).notNull(), operator: varchar("operator", { length: 40 }), businessFieldId: varchar("business_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "restrict" }), literalValue: longtext("literal_value"), config: longtext("config"), sortOrder: int("sort_order").notNull().default(0), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_formula_nodes_kpi_idx").on(t.kpiId), index("kpi_formula_nodes_parent_idx").on(t.parentNodeId)]);

export const kpiSourceFields = mysqlTable("kpi_source_fields", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), businessObjectId: varchar("business_object_id", { length: 36 }).notNull().references(() => businessObjects.id, { onDelete: "restrict" }), businessFieldId: varchar("business_field_id", { length: 36 }).notNull().references(() => businessFields.id, { onDelete: "restrict" }), role: mysqlEnum("role", ["MEASURE", "DIMENSION", "FILTER", "DATE", "JOIN"]).notNull(), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("kpi_source_fields_uq").on(t.kpiId, t.businessFieldId, t.role), index("kpi_source_fields_kpi_idx").on(t.kpiId)]);

export const kpiFilters = mysqlTable("kpi_filters", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), businessFieldId: varchar("business_field_id", { length: 36 }).notNull().references(() => businessFields.id, { onDelete: "restrict" }), operator: varchar("operator", { length: 32 }).notNull(), values: longtext("values"), isRequired: boolean("is_required").notNull().default(false), isExclusion: boolean("is_exclusion").notNull().default(false), sortOrder: int("sort_order").notNull().default(0), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_filters_kpi_idx").on(t.kpiId)]);

export const kpiThresholds = mysqlTable("kpi_thresholds", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), thresholdType: mysqlEnum("threshold_type", ["TARGET", "WARNING", "CRITICAL", "GOOD_RANGE", "BAD_RANGE"]).notNull(), operator: varchar("operator", { length: 24 }), value: varchar("value", { length: 100 }), lowerValue: varchar("lower_value", { length: 100 }), upperValue: varchar("upper_value", { length: 100 }), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_thresholds_kpi_idx").on(t.kpiId)]);

export const kpiValidationResults = mysqlTable("kpi_validation_results", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), result: mysqlEnum("result", validationOutcomeValues).notNull(), ruleCode: varchar("rule_code", { length: 80 }).notNull(), severity: mysqlEnum("severity", ["INFO", "WARNING", "ERROR"]).notNull(), message: text("message").notNull(), businessObjectId: varchar("business_object_id", { length: 36 }).references(() => businessObjects.id, { onDelete: "set null" }), businessFieldId: varchar("business_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "set null" }), suggestedFix: text("suggested_fix"), validatedBy: varchar("validated_by", { length: 36 }).notNull(), validatedAt: datetime("validated_at", { mode: "date", fsp: 3 }).notNull(), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_validation_results_kpi_idx").on(t.kpiId), index("kpi_validation_results_rule_idx").on(t.ruleCode)]);

export const businessContextModelVersions = mysqlTable("business_context_model_versions", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), parentVersionId: varchar("parent_version_id", { length: 36 }), versionNumber: int("version_number").notNull(), changeSummary: text("change_summary"), status: mysqlEnum("status", ["DRAFT", "UNDER_REVIEW", "APPROVED", "PUBLISHED", "SUPERSEDED"]).notNull().default("DRAFT"), objectsSnapshot: longtext("objects_snapshot").notNull(), fieldsSnapshot: longtext("fields_snapshot").notNull(), relationshipsSnapshot: longtext("relationships_snapshot").notNull(), kpisSnapshot: longtext("kpis_snapshot").notNull(), glossarySnapshot: longtext("glossary_snapshot").notNull(), reviewedBy: varchar("reviewed_by", { length: 36 }), approvedBy: varchar("approved_by", { length: 36 }), publishedBy: varchar("published_by", { length: 36 }), publishedAt: datetime("published_at", { mode: "date", fsp: 3 }), createdBy: varchar("created_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("business_context_versions_uq").on(t.modelId, t.versionNumber), index("business_context_versions_model_idx").on(t.modelId)]);

export const aiBusinessContextAnalysisJobs = mysqlTable("ai_business_context_analysis_jobs", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), status: mysqlEnum("status", ["QUEUED", "RUNNING", "COMPLETED", "FAILED", "CANCELLED"]).notNull().default("QUEUED"), provider: varchar("provider", { length: 80 }).notNull(), promptVersion: varchar("prompt_version", { length: 40 }).notNull(), inputHash: varchar("input_hash", { length: 64 }).notNull(), progressStep: varchar("progress_step", { length: 120 }), recommendationCount: int("recommendation_count").notNull().default(0), inputTokenCount: int("input_token_count").notNull().default(0), outputTokenCount: int("output_token_count").notNull().default(0), retryCount: int("retry_count").notNull().default(0), redactedInput: longtext("redacted_input"), errorSummary: text("error_summary"), startedBy: varchar("started_by", { length: 36 }).notNull(), startedAt: datetime("started_at", { mode: "date", fsp: 3 }).notNull(), completedAt: datetime("completed_at", { mode: "date", fsp: 3 }), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("ai_analysis_jobs_model_idx").on(t.modelId), index("ai_analysis_jobs_started_idx").on(t.startedAt)]);

export const aiBusinessContextRecommendations = mysqlTable("ai_business_context_recommendations", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), jobId: varchar("job_id", { length: 36 }).notNull().references(() => aiBusinessContextAnalysisJobs.id, { onDelete: "restrict" }), recommendationType: mysqlEnum("recommendation_type", ["OBJECT_NAME", "OBJECT_DESCRIPTION", "BUSINESS_DOMAIN", "RECORD_GRAIN", "FIELD_NAME", "BUSINESS_TYPE", "FIELD_ROLE", "AGGREGATION", "RELATIONSHIP", "SYNONYM", "POTENTIAL_KPI", "IGNORE_OBJECT", "SENSITIVE_FIELD"]).notNull(), targetType: mysqlEnum("target_type", ["MODEL", "OBJECT", "FIELD", "RELATIONSHIP", "KPI"]).notNull(), targetId: varchar("target_id", { length: 36 }), currentValue: longtext("current_value"), suggestedValue: longtext("suggested_value").notNull(), reason: text("reason").notNull(), confidenceScore: int("confidence_score").notNull(), evidence: longtext("evidence"), impact: mysqlEnum("impact", ["LOW", "MEDIUM", "HIGH"]).notNull().default("MEDIUM"), status: mysqlEnum("status", ["PENDING", "ACCEPTED", "REJECTED", "MODIFIED", "SUPERSEDED"]).notNull().default("PENDING"), reviewedBy: varchar("reviewed_by", { length: 36 }), reviewedAt: datetime("reviewed_at", { mode: "date", fsp: 3 }), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("ai_recommendations_model_idx").on(t.modelId), index("ai_recommendations_status_idx").on(t.status)]);

export const businessGlossaryTerms = mysqlTable("business_glossary_terms", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).references(() => businessContextModels.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).references(() => dataSources.id, { onDelete: "restrict" }), businessDomainId: varchar("business_domain_id", { length: 36 }).references(() => businessDomains.id, { onDelete: "set null" }), businessObjectId: varchar("business_object_id", { length: 36 }).references(() => businessObjects.id, { onDelete: "set null" }), businessFieldId: varchar("business_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "set null" }), kpiId: varchar("kpi_id", { length: 36 }).references(() => kpiDefinitions.id, { onDelete: "set null" }), term: varchar("term", { length: 160 }).notNull(), definition: text("definition").notNull(), abbreviations: longtext("abbreviations"), language: mysqlEnum("language", ["EN", "TH"]).notNull().default("EN"), approvalStatus: mysqlEnum("approval_status", approvalStatusValues).notNull().default("DRAFT"), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("business_glossary_model_idx").on(t.modelId), index("business_glossary_term_idx").on(t.term)]);

export const businessSynonyms = mysqlTable("business_synonyms", {
  id: varchar("id", { length: 36 }).primaryKey(), glossaryTermId: varchar("glossary_term_id", { length: 36 }).references(() => businessGlossaryTerms.id, { onDelete: "restrict" }), businessObjectId: varchar("business_object_id", { length: 36 }).references(() => businessObjects.id, { onDelete: "restrict" }), businessFieldId: varchar("business_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "restrict" }), kpiId: varchar("kpi_id", { length: 36 }).references(() => kpiDefinitions.id, { onDelete: "restrict" }), synonym: varchar("synonym", { length: 160 }).notNull(), language: mysqlEnum("language", ["EN", "TH"]).notNull().default("EN"), normalizedValue: varchar("normalized_value", { length: 190 }).notNull(), version: int("version").notNull().default(1), status: mysqlEnum("status", approvalStatusValues).notNull().default("DRAFT"), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("business_synonyms_normalized_idx").on(t.normalizedValue), index("business_synonyms_term_idx").on(t.glossaryTermId)]);

export const kpiTestCases = mysqlTable("kpi_test_cases", {
  id: varchar("id", { length: 36 }).primaryKey(), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), name: varchar("name", { length: 190 }).notNull(), inputFilters: longtext("input_filters"), expectedResult: varchar("expected_result", { length: 255 }), tolerance: varchar("tolerance", { length: 100 }), technicalValidationNotes: text("technical_validation_notes"), businessValidationNotes: text("business_validation_notes"), status: mysqlEnum("status", ["DRAFT", "ACTIVE", "ARCHIVED"]).notNull().default("DRAFT"), version: int("version").notNull().default(1), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_test_cases_kpi_idx").on(t.kpiId)]);

export const kpiTestResults = mysqlTable("kpi_test_results", {
  id: varchar("id", { length: 36 }).primaryKey(), testCaseId: varchar("test_case_id", { length: 36 }).references(() => kpiTestCases.id, { onDelete: "restrict" }), kpiId: varchar("kpi_id", { length: 36 }).notNull().references(() => kpiDefinitions.id, { onDelete: "restrict" }), inputFilters: longtext("input_filters"), expectedResult: varchar("expected_result", { length: 255 }), actualResult: varchar("actual_result", { length: 255 }), difference: varchar("difference", { length: 255 }), tolerance: varchar("tolerance", { length: 100 }), status: mysqlEnum("status", ["PASSED", "FAILED", "ERROR", "CANCELLED"]).notNull(), generatedSql: longtext("generated_sql"), rowCount: int("row_count").notNull().default(0), resultPreview: longtext("result_preview"), durationMs: int("duration_ms"), errorDetail: text("error_detail"), testedBy: varchar("tested_by", { length: 36 }).notNull(), testedAt: datetime("tested_at", { mode: "date", fsp: 3 }).notNull(), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("kpi_test_results_kpi_idx").on(t.kpiId), index("kpi_test_results_case_idx").on(t.testCaseId)]);

export const businessContextReviewRequests = mysqlTable("business_context_review_requests", {
  id: varchar("id", { length: 36 }).primaryKey(), modelId: varchar("model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), modelVersionId: varchar("model_version_id", { length: 36 }).references(() => businessContextModelVersions.id, { onDelete: "restrict" }), kpiId: varchar("kpi_id", { length: 36 }).references(() => kpiDefinitions.id, { onDelete: "restrict" }), reviewStage: mysqlEnum("review_stage", ["DATA_STEWARD_REVIEW", "TECHNICAL_VALIDATION", "BUSINESS_OWNER_REVIEW"]).notNull(), status: mysqlEnum("status", ["OPEN", "APPROVED", "REJECTED", "CHANGES_REQUESTED", "CANCELLED"]).notNull().default("OPEN"), requestedBy: varchar("requested_by", { length: 36 }).notNull(), assignedTo: varchar("assigned_to", { length: 36 }), requestedAt: datetime("requested_at", { mode: "date", fsp: 3 }).notNull(), completedAt: datetime("completed_at", { mode: "date", fsp: 3 }), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("business_context_review_requests_model_idx").on(t.modelId), index("business_context_review_requests_status_idx").on(t.status)]);

export const businessContextReviewActions = mysqlTable("business_context_review_actions", {
  id: varchar("id", { length: 36 }).primaryKey(), reviewRequestId: varchar("review_request_id", { length: 36 }).notNull().references(() => businessContextReviewRequests.id, { onDelete: "restrict" }), action: mysqlEnum("action", ["APPROVE", "REJECT", "REQUEST_CHANGES", "COMMENT"]).notNull(), comment: text("comment"), changedFields: longtext("changed_fields"), modelVersion: int("model_version").notNull(), reviewerId: varchar("reviewer_id", { length: 36 }).notNull(), actionAt: datetime("action_at", { mode: "date", fsp: 3 }).notNull(), version: int("version").notNull().default(1), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("business_context_review_actions_request_idx").on(t.reviewRequestId), index("business_context_review_actions_reviewer_idx").on(t.reviewerId)]);

export const dashboardStatusValues = ["DRAFT", "VALIDATING", "READY_FOR_REVIEW", "IN_REVIEW", "CHANGES_REQUESTED", "APPROVED", "PUBLISHED", "UNPUBLISHED", "ARCHIVED"] as const;
export const dashboardVersionStatusValues = ["DRAFT", "READY_FOR_REVIEW", "IN_REVIEW", "CHANGES_REQUESTED", "APPROVED", "PUBLISHED", "SUPERSEDED"] as const;
export const dashboardBlockTypeValues = ["KPI_CARD", "TREND_CHART", "COMPARISON_CHART", "DISTRIBUTION_CHART", "PROGRESS_STATUS", "TABLE", "PIVOT_TABLE", "FUNNEL", "EXCEPTION_LIST", "TEXT_INSIGHT", "FILTER"] as const;
export const dashboardVisualizationValues = ["NUMBER", "LINE", "AREA", "BAR", "HORIZONTAL_BAR", "STACKED_BAR", "DONUT", "PIE", "TREEMAP", "PROGRESS", "GAUGE", "BULLET", "TABLE", "PIVOT", "FUNNEL", "EXCEPTION_LIST", "TEXT"] as const;

export const dashboardLayoutTemplates = mysqlTable("dashboard_layout_templates", {
  id: varchar("id", { length: 36 }).primaryKey(), code: varchar("code", { length: 80 }).notNull(), name: varchar("name", { length: 160 }).notNull(), description: text("description"), gridColumns: int("grid_columns").notNull().default(12), definitionJson: longtext("definition_json").notNull(), isActive: boolean("is_active").notNull().default(true), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("dashboard_layout_templates_code_uq").on(t.code)]);

export const dashboards = mysqlTable("dashboards", {
  id: varchar("id", { length: 36 }).primaryKey(), name: varchar("name", { length: 190 }).notNull(), slug: varchar("slug", { length: 190 }), description: text("description"), category: varchar("category", { length: 80 }).notNull(), thumbnailUrl: text("thumbnail_url"), ownerUserId: varchar("owner_user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "restrict" }), businessOwnerUserId: varchar("business_owner_user_id", { length: 36 }).references(() => users.id, { onDelete: "set null" }), currentDraftVersionId: varchar("current_draft_version_id", { length: 36 }), currentPublishedVersionId: varchar("current_published_version_id", { length: 36 }), status: mysqlEnum("status", dashboardStatusValues).notNull().default("DRAFT"), visibility: mysqlEnum("visibility", ["PRIVATE", "ROLE", "WORKSPACE"]).notNull().default("PRIVATE"), exportAllowed: boolean("export_allowed").notNull().default(false), aiCopilotAllowed: boolean("ai_copilot_allowed").notNull().default(true), underlyingDataAllowed: boolean("underlying_data_allowed").notNull().default(false), drillDownAllowed: boolean("drill_down_allowed").notNull().default(false), isFeatured: boolean("is_featured").notNull().default(false), viewCount: int("view_count").notNull().default(0), lastDataRefreshAt: datetime("last_data_refresh_at", { mode: "date", fsp: 3 }), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), archivedAt: datetime("archived_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("dashboards_slug_uq").on(t.slug), index("dashboards_owner_idx").on(t.ownerUserId), index("dashboards_status_idx").on(t.status)]);

export const dashboardVersions = mysqlTable("dashboard_versions", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "restrict" }), versionNumber: int("version_number").notNull(), businessObjective: text("business_objective").notNull(), targetAudience: varchar("target_audience", { length: 500 }).notNull(), businessQuestionsJson: longtext("business_questions_json").notNull(), refreshExpectation: varchar("refresh_expectation", { length: 120 }).notNull(), defaultDateRange: varchar("default_date_range", { length: 80 }).notNull(), tagsJson: longtext("tags_json"), businessContextModelId: varchar("business_context_model_id", { length: 36 }).notNull().references(() => businessContextModels.id, { onDelete: "restrict" }), businessContextVersionId: varchar("business_context_version_id", { length: 36 }).notNull().references(() => businessContextModelVersions.id, { onDelete: "restrict" }), dataSourceId: varchar("data_source_id", { length: 36 }).notNull().references(() => dataSources.id, { onDelete: "restrict" }), layoutTemplateId: varchar("layout_template_id", { length: 36 }).references(() => dashboardLayoutTemplates.id, { onDelete: "restrict" }), layoutJson: longtext("layout_json").notNull(), status: mysqlEnum("status", dashboardVersionStatusValues).notNull().default("DRAFT"), revision: int("revision").notNull().default(1), changeSummary: text("change_summary"), submittedAt: datetime("submitted_at", { mode: "date", fsp: 3 }), submittedBy: varchar("submitted_by", { length: 36 }), approvedAt: datetime("approved_at", { mode: "date", fsp: 3 }), approvedBy: varchar("approved_by", { length: 36 }), publishedAt: datetime("published_at", { mode: "date", fsp: 3 }), publishedBy: varchar("published_by", { length: 36 }), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [uniqueIndex("dashboard_versions_number_uq").on(t.dashboardId, t.versionNumber), index("dashboard_versions_dashboard_idx").on(t.dashboardId), index("dashboard_versions_status_idx").on(t.status)]);

export const dashboardBlocks = mysqlTable("dashboard_blocks", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "cascade" }), blockType: mysqlEnum("block_type", dashboardBlockTypeValues).notNull(), title: varchar("title", { length: 190 }).notNull(), description: text("description"), businessQuestion: text("business_question"), intendedAudience: varchar("intended_audience", { length: 500 }), decisionSupported: text("decision_supported"), kpiId: varchar("kpi_id", { length: 36 }).references(() => kpiDefinitions.id, { onDelete: "restrict" }), kpiVersion: int("kpi_version"), dimensionFieldId: varchar("dimension_field_id", { length: 36 }).references(() => businessFields.id, { onDelete: "restrict" }), visualizationType: mysqlEnum("visualization_type", dashboardVisualizationValues).notNull(), queryPlanJson: longtext("query_plan_json"), generatedSql: longtext("generated_sql"), queryFingerprint: varchar("query_fingerprint", { length: 64 }), bindParametersJson: longtext("bind_parameters_json"), filtersJson: longtext("filters_json"), visualizationConfigJson: longtext("visualization_config_json"), formattingConfigJson: longtext("formatting_config_json"), positionJson: longtext("position_json").notNull(), validationStatus: mysqlEnum("validation_status", ["NOT_VALIDATED", "PASSED", "PASSED_WITH_WARNING", "FAILED"]).notNull().default("NOT_VALIDATED"), previewStatus: mysqlEnum("preview_status", ["NOT_RUN", "PASSED", "FAILED", "UNAVAILABLE"]).notNull().default("NOT_RUN"), previewJson: longtext("preview_json"), previewedAt: datetime("previewed_at", { mode: "date", fsp: 3 }), isHidden: boolean("is_hidden").notNull().default(false), isLocked: boolean("is_locked").notNull().default(false), sortOrder: int("sort_order").notNull().default(0), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_blocks_version_idx").on(t.dashboardVersionId), index("dashboard_blocks_kpi_idx").on(t.kpiId)]);

export const dashboardGlobalFilters = mysqlTable("dashboard_global_filters", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "cascade" }), name: varchar("name", { length: 160 }).notNull(), businessFieldId: varchar("business_field_id", { length: 36 }).notNull().references(() => businessFields.id, { onDelete: "restrict" }), filterType: mysqlEnum("filter_type", ["DATE_RANGE", "SINGLE_SELECT", "MULTI_SELECT", "NUMERIC_RANGE", "BOOLEAN"]).notNull(), defaultValueJson: longtext("default_value_json"), allowedValuesJson: longtext("allowed_values_json"), appliesToBlockIdsJson: longtext("applies_to_block_ids_json"), isRequired: boolean("is_required").notNull().default(false), isVisible: boolean("is_visible").notNull().default(true), runtimeEditable: boolean("runtime_editable").notNull().default(true), securityEnforced: boolean("security_enforced").notNull().default(false), createdBy: varchar("created_by", { length: 36 }).notNull(), updatedBy: varchar("updated_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_global_filters_version_idx").on(t.dashboardVersionId)]);

export const dashboardValidationFindings = mysqlTable("dashboard_validation_findings", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "cascade" }), dashboardBlockId: varchar("dashboard_block_id", { length: 36 }).references(() => dashboardBlocks.id, { onDelete: "cascade" }), category: mysqlEnum("category", ["CONFIGURATION", "BUSINESS_CONTEXT", "KPI", "QUERY", "QUALITY", "ACCESS"]).notNull(), severity: mysqlEnum("severity", ["INFO", "WARNING", "ERROR"]).notNull(), code: varchar("code", { length: 80 }).notNull(), message: text("message").notNull(), suggestedCorrection: text("suggested_correction"), isResolved: boolean("is_resolved").notNull().default(false), resolvedAt: datetime("resolved_at", { mode: "date", fsp: 3 }), resolvedBy: varchar("resolved_by", { length: 36 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_validation_version_idx").on(t.dashboardVersionId), index("dashboard_validation_block_idx").on(t.dashboardBlockId)]);

export const dashboardAiRecommendations = mysqlTable("dashboard_ai_recommendations", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "cascade" }), dashboardBlockId: varchar("dashboard_block_id", { length: 36 }).references(() => dashboardBlocks.id, { onDelete: "cascade" }), recommendationType: mysqlEnum("recommendation_type", ["BLOCK_CONFIGURATION", "KPI", "DIMENSION", "VISUALIZATION", "LAYOUT", "FILTER", "COVERAGE", "REDUNDANCY"]).notNull(), requestJson: longtext("request_json").notNull(), contextJson: longtext("context_json").notNull(), responseJson: longtext("response_json").notNull(), confidence: int("confidence").notNull(), modelProvider: varchar("model_provider", { length: 80 }).notNull(), modelName: varchar("model_name", { length: 120 }).notNull(), decision: mysqlEnum("decision", ["PENDING", "ACCEPTED", "REJECTED", "MODIFIED"]).notNull().default("PENDING"), decidedBy: varchar("decided_by", { length: 36 }), decidedAt: datetime("decided_at", { mode: "date", fsp: 3 }), createdBy: varchar("created_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_ai_version_idx").on(t.dashboardVersionId), index("dashboard_ai_decision_idx").on(t.decision)]);

export const dashboardReviews = mysqlTable("dashboard_reviews", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "restrict" }), dashboardBlockId: varchar("dashboard_block_id", { length: 36 }).references(() => dashboardBlocks.id, { onDelete: "restrict" }), action: mysqlEnum("action", ["SUBMIT", "COMMENT", "APPROVE", "REJECT", "REQUEST_CHANGES", "ACKNOWLEDGE_WARNING"]).notNull(), comment: text("comment"), reviewerId: varchar("reviewer_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "restrict" }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_reviews_version_idx").on(t.dashboardVersionId)]);

export const dashboardPublications = mysqlTable("dashboard_publications", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "restrict" }), dashboardVersionId: varchar("dashboard_version_id", { length: 36 }).notNull().references(() => dashboardVersions.id, { onDelete: "restrict" }), snapshotJson: longtext("snapshot_json").notNull(), visibility: mysqlEnum("visibility", ["PRIVATE", "ROLE", "WORKSPACE"]).notNull(), allowedRolesJson: longtext("allowed_roles_json"), allowedUserIdsJson: longtext("allowed_user_ids_json"), viewerConfigurationJson: longtext("viewer_configuration_json"), exportAllowed: boolean("export_allowed").notNull().default(false), aiCopilotAllowed: boolean("ai_copilot_allowed").notNull().default(false), underlyingDataAllowed: boolean("underlying_data_allowed").notNull().default(false), drillDownAllowed: boolean("drill_down_allowed").notNull().default(false), publishedBy: varchar("published_by", { length: 36 }).notNull().references(() => users.id, { onDelete: "restrict" }), publishedAt: datetime("published_at", { mode: "date", fsp: 3 }).notNull(), unpublishedAt: datetime("unpublished_at", { mode: "date", fsp: 3 }),
}, (t) => [uniqueIndex("dashboard_publications_version_uq").on(t.dashboardVersionId), index("dashboard_publications_dashboard_idx").on(t.dashboardId)]);

export const dashboardPermissions = mysqlTable("dashboard_permissions", {
  id: varchar("id", { length: 36 }).primaryKey(), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), userId: varchar("user_id", { length: 36 }).references(() => users.id, { onDelete: "cascade" }), role: mysqlEnum("role", roleValues), permission: mysqlEnum("permission", ["VIEW", "EXPORT", "UNDERLYING_DATA", "USE_AI", "EXECUTIVE_SUMMARY"]).notNull(), grantedBy: varchar("granted_by", { length: 36 }).notNull(), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), revokedAt: datetime("revoked_at", { mode: "date", fsp: 3 }),
}, (t) => [index("dashboard_permissions_dashboard_idx").on(t.dashboardId), index("dashboard_permissions_user_idx").on(t.userId)]);

export const dashboardFavorites = mysqlTable("dashboard_favorites", {
  userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [primaryKey({ columns: [t.userId, t.dashboardId] }), index("dashboard_favorites_dashboard_idx").on(t.dashboardId)]);

export const dashboardRecentViews = mysqlTable("dashboard_recent_views", {
  userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), lastViewedAt: datetime("last_viewed_at", { mode: "date", fsp: 3 }).notNull(), viewCount: int("view_count").notNull().default(1),
}, (t) => [primaryKey({ columns: [t.userId, t.dashboardId] }), index("dashboard_recent_views_last_idx").on(t.lastViewedAt)]);

export const dashboardViewEvents = mysqlTable("dashboard_view_events", {
  id: varchar("id", { length: 36 }).primaryKey(), userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), eventType: mysqlEnum("event_type", ["VIEW", "FILTER_APPLY", "FILTER_CLEAR", "WIDGET_RETRY", "FAVORITE", "UNFAVORITE", "AI_CHAT", "EXECUTIVE_SUMMARY", "EXPORT"]).notNull(), widgetId: varchar("widget_id", { length: 36 }), parametersJson: longtext("parameters_json"), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("dashboard_view_events_dashboard_idx").on(t.dashboardId), index("dashboard_view_events_created_idx").on(t.createdAt)]);

export const aiConversations = mysqlTable("ai_conversations", {
  id: varchar("id", { length: 36 }).primaryKey(), userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), title: varchar("title", { length: 190 }).notNull(), filtersSnapshotJson: longtext("filters_snapshot_json"), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(), updatedAt: datetime("updated_at", { mode: "date", fsp: 3 }).notNull(), deletedAt: datetime("deleted_at", { mode: "date", fsp: 3 }),
}, (t) => [index("ai_conversations_user_idx").on(t.userId), index("ai_conversations_dashboard_idx").on(t.dashboardId)]);

export const aiMessages = mysqlTable("ai_messages", {
  id: varchar("id", { length: 36 }).primaryKey(), conversationId: varchar("conversation_id", { length: 36 }).notNull().references(() => aiConversations.id, { onDelete: "cascade" }), role: mysqlEnum("role", ["USER", "ASSISTANT"]).notNull(), content: longtext("content").notNull(), widgetId: varchar("widget_id", { length: 36 }), filtersSnapshotJson: longtext("filters_snapshot_json"), model: varchar("model", { length: 120 }), inputTokens: int("input_tokens"), outputTokens: int("output_tokens"), latencyMs: int("latency_ms"), errorCode: varchar("error_code", { length: 80 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("ai_messages_conversation_idx").on(t.conversationId, t.createdAt)]);

export const aiUsageLogs = mysqlTable("ai_usage_logs", {
  id: varchar("id", { length: 36 }).primaryKey(), userId: varchar("user_id", { length: 36 }).notNull().references(() => users.id, { onDelete: "cascade" }), dashboardId: varchar("dashboard_id", { length: 36 }).notNull().references(() => dashboards.id, { onDelete: "cascade" }), conversationId: varchar("conversation_id", { length: 36 }).references(() => aiConversations.id, { onDelete: "set null" }), requestType: mysqlEnum("request_type", ["CHAT", "EXECUTIVE_SUMMARY"]).notNull(), model: varchar("model", { length: 120 }).notNull(), inputTokens: int("input_tokens").notNull().default(0), outputTokens: int("output_tokens").notNull().default(0), latencyMs: int("latency_ms").notNull().default(0), status: mysqlEnum("status", ["SUCCESS", "FAILED", "REFUSED"]).notNull(), errorCode: varchar("error_code", { length: 80 }), createdAt: datetime("created_at", { mode: "date", fsp: 3 }).notNull(),
}, (t) => [index("ai_usage_logs_dashboard_idx").on(t.dashboardId), index("ai_usage_logs_created_idx").on(t.createdAt)]);
