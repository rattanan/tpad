import { notFound, redirect } from "next/navigation"; import BusinessContextWorkspace from "@/components/business-context/workspace"; import { getCurrentSession } from "@/lib/auth/session"; import { getBusinessContextWorkspace } from "@/lib/business-context/service";
export default async function BusinessContextModelPage({ params }: { params: Promise<{ modelId: string }> }) { const session=await getCurrentSession();if(!session)redirect("/login");let data;try{data=await getBusinessContextWorkspace((await params).modelId,session.user);}catch(error){if(error instanceof Error&&error.message.includes("not found"))notFound();throw error;}return <BusinessContextWorkspace dataJson={JSON.stringify(data)} role={session.user.role}/>; }
