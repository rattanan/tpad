import { redirect } from "next/navigation"; import KpiCatalogue from "@/components/kpis/kpi-catalogue"; import { getCurrentSession } from "@/lib/auth/session"; import { getBusinessContextWorkspace, listBusinessContextModels, listKpis } from "@/lib/business-context/service";
export default async function KpiCataloguePage({searchParams}:{searchParams:Promise<{modelId?:string}>}){const session=await getCurrentSession();if(!session)redirect('/login');const kpis=await listKpis(session.user,{});let models:Awaited<ReturnType<typeof listBusinessContextModels>>['items']=[];let contextJson='';if(session.user.role!=='VIEWER'){models=(await listBusinessContextModels(session.user,{page:1,pageSize:100})).items;const requested=(await searchParams).modelId;const selected=models.find((item)=>item.id===requested)?.id??models[0]?.id;if(selected)contextJson=JSON.stringify(await getBusinessContextWorkspace(selected,session.user));}return <KpiCatalogue initialKpisJson={JSON.stringify(kpis)} models={models.map((item)=>({id:item.id,name:item.name,status:item.status}))} contextJson={contextJson} role={session.user.role}/>;}
