import { redirect } from "next/navigation"; import MetadataExplorer from "@/components/data-sources/metadata-explorer"; import { getCurrentSession } from "@/lib/auth/session"; import { canAccessDataSource } from "@/lib/data-sources/service";
export default async function MetadataPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const session = await getCurrentSession(); if (!session) redirect("/login"); const canSync = await canAccessDataSource(session.user, id, "SYNC_METADATA"); return <main className="ds-page ds-full"><p className="eyebrow">METADATA EXPLORER</p><h1>Oracle metadata</h1><p className="intro">Browse synchronized tables, views, columns and relationships.</p><MetadataExplorer id={id} canSync={canSync} /></main>; }
