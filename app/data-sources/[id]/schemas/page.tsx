import { redirect } from "next/navigation"; import SchemaSelector from "@/components/data-sources/schema-selector"; import { getCurrentSession } from "@/lib/auth/session"; import { canAccessDataSource } from "@/lib/data-sources/service";
export default async function SchemasPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const session = await getCurrentSession(); if (!session) redirect("/login"); if (!await canAccessDataSource(session.user, id, "SYNC_METADATA")) redirect(`/data-sources/${id}/metadata?error=forbidden`); return <main className="ds-page"><p className="eyebrow">DATA SOURCE / SCHEMAS</p><h1>Select schemas</h1><p className="intro">System schemas are excluded by default and can be configured with ORACLE_EXCLUDED_SCHEMAS.</p><SchemaSelector id={id} /></main>; }
