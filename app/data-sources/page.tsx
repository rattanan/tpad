import Link from "next/link"; import SourceList from "@/components/data-sources/source-list"; import { getCurrentSession } from "@/lib/auth/session"; import { canCreateDataSource } from "@/lib/auth/permissions";
export default async function DataSourcesPage() { const session = await getCurrentSession(); const canCreate = session ? canCreateDataSource(session.user.role) : false; return <main className="ds-page"><div className="title-row"><div><p className="eyebrow">DATA MANAGEMENT</p><h1>Data Sources</h1><p className="intro">Manage Oracle connections and synchronized metadata.</p></div>{canCreate && <Link className="primary-button" href="/data-sources/new">＋ New data source</Link>}</div><SourceList /></main>; }
