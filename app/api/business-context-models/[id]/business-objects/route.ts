import type { NextRequest } from "next/server"; import { requireSession } from "@/lib/auth/session"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { writeAudit } from "@/lib/auth/audit"; import { apiError, HttpError } from "@/lib/http"; import { createBusinessObject, getBusinessContextWorkspace } from "@/lib/business-context/service"; import { objectCreateSchema } from "@/lib/business-context/validation";
export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { const session = await requireSession(request); const workspace = await getBusinessContextWorkspace((await params).id, session.user); return Response.json({ items: workspace.objects }); } catch (error) { return apiError(error, meta.requestId); } }
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); const object = await createBusinessObject((await params).id, objectCreateSchema.parse(await request.json()), session.user); await writeAudit({ actor: session.user, action: "CREATE_BUSINESS_OBJECT", category: "BUSINESS_CONTEXT", targetType: "BUSINESS_OBJECT", targetId: object.id, targetName: object.businessName, newValues: object, meta }); return Response.json({ object }, { status: 201 }); } catch (error) { return apiError(error, meta.requestId); } }
