import type { NextRequest } from "next/server";import { requireSession } from "@/lib/auth/session";import { getRequestMeta } from "@/lib/auth/request";import { apiError,HttpError } from "@/lib/http";import { listDashboardConversations } from "@/lib/published-dashboards/copilot";
export async function GET(request:NextRequest){const meta=getRequestMeta(request);try{const session=await requireSession(request);const slug=request.nextUrl.searchParams.get("dashboard");if(!slug)throw new HttpError(400,"Dashboard is required","VALIDATION_ERROR");return Response.json({conversations:await listDashboardConversations(slug,session.user)});}catch(error){return apiError(error,meta.requestId)}}
