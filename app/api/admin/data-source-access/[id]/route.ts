import { type NextRequest } from "next/server"; import { apiError, HttpError } from "@/lib/http"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { requireSession } from "@/lib/auth/session"; import { canAssignDataSourceAccess } from "@/lib/auth/permissions"; import { revokeDataSourceAccess } from "@/lib/data-sources/service"; import { writeAudit } from "@/lib/auth/audit";
export async function DELETE(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); if (!canAssignDataSourceAccess(session.user.role)) throw new HttpError(403, "Forbidden", "FORBIDDEN"); const id = (await params).id; await revokeDataSourceAccess(id); await writeAudit({ actor: session.user, action: "DATA_SOURCE_ACCESS_REVOKED", category: "DATA_SOURCE", targetType: "DATA_SOURCE_ACCESS", targetId: id, meta }); return Response.json({ success: true }); } catch (error) { return apiError(error, meta.requestId); } }
