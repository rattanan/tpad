import type { NextRequest } from "next/server"; import { requireSession } from "@/lib/auth/session"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { writeAudit } from "@/lib/auth/audit"; import { apiError, HttpError } from "@/lib/http"; import { updateBusinessRelationship } from "@/lib/business-context/service"; import { relationshipUpdateSchema } from "@/lib/business-context/validation";
export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); const id = (await params).id; const relationship = await updateBusinessRelationship(id, relationshipUpdateSchema.parse(await request.json()), session.user); await writeAudit({ actor: session.user, action: "UPDATE_RELATIONSHIP", category: "BUSINESS_CONTEXT", targetType: "BUSINESS_RELATIONSHIP", targetId: id, newValues: relationship, meta }); return Response.json({ relationship }); } catch (error) { return apiError(error, meta.requestId); } }
