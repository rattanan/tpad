import { type NextRequest } from "next/server"; import { apiError } from "@/lib/http"; import { getRequestMeta } from "@/lib/auth/request"; import { requireSession } from "@/lib/auth/session"; import { requireDataSourceAccess } from "@/lib/data-sources/service"; import { listSyncRuns } from "@/lib/data-sources/catalog";
export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { const id = (await params).id; const session = await requireSession(request); await requireDataSourceAccess(session.user, id, "VIEW_METADATA"); return Response.json({ items: await listSyncRuns(id) }); } catch (error) { return apiError(error, meta.requestId); } }
