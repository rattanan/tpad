import { type NextRequest } from "next/server"; import { apiError, HttpError } from "@/lib/http"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { requireSession } from "@/lib/auth/session"; import { archiveDataSource } from "@/lib/data-sources/service"; import { writeAudit } from "@/lib/auth/audit";
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); const source = await archiveDataSource((await params).id, session.user); await writeAudit({ actor: session.user, action: "DATA_SOURCE_ARCHIVED", category: "DATA_SOURCE", targetType: "DATA_SOURCE", targetId: source.id, targetName: source.name, meta }); return Response.json({ success: true }); } catch (error) { return apiError(error, meta.requestId); } }
