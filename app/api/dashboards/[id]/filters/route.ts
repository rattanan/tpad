import { type NextRequest } from "next/server"; import { requireSession } from "@/lib/auth/session"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { apiError, HttpError } from "@/lib/http"; import { writeAudit } from "@/lib/auth/audit"; import { globalFilterSchema } from "@/lib/dashboards/validation"; import { addGlobalFilter } from "@/lib/dashboards/service";
export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); const id = (await params).id; const input = globalFilterSchema.parse(await request.json()); const result = await addGlobalFilter(id, input, session.user); await writeAudit({ actor: session.user, action: "DASHBOARD_FILTER_ADDED", category: "DASHBOARD", targetType: "DASHBOARD_FILTER", targetId: result.id, newValues: { dashboardId: id, fieldId: input.businessFieldId, securityEnforced: input.securityEnforced }, meta }); return Response.json(result, { status: 201 }); } catch (error) { return apiError(error, meta.requestId); } }
