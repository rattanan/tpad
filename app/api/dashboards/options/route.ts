import { type NextRequest } from "next/server"; import { requireSession } from "@/lib/auth/session"; import { getRequestMeta } from "@/lib/auth/request"; import { apiError } from "@/lib/http"; import { listDashboardTemplates, listPublishedContexts } from "@/lib/dashboards/service";
export async function GET(request: NextRequest) { const meta = getRequestMeta(request); try { const session = await requireSession(request); const [templates, contexts] = await Promise.all([listDashboardTemplates(), listPublishedContexts(session.user)]); return Response.json({ templates, contexts }); } catch (error) { return apiError(error, meta.requestId); } }
