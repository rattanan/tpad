import { type NextRequest } from "next/server";
import { requireSession } from "@/lib/auth/session"; import { getRequestMeta, isSameOrigin } from "@/lib/auth/request"; import { apiError, HttpError } from "@/lib/http"; import { writeAudit } from "@/lib/auth/audit"; import { dashboardCreateSchema } from "@/lib/dashboards/validation"; import { createDashboard, listDashboards } from "@/lib/dashboards/service";
export async function GET(request: NextRequest) { const meta = getRequestMeta(request); try { const session = await requireSession(request); return Response.json({ items: await listDashboards(session.user) }); } catch (error) { return apiError(error, meta.requestId); } }
export async function POST(request: NextRequest) { const meta = getRequestMeta(request); try { if (!isSameOrigin(request)) throw new HttpError(403, "Invalid request origin", "CSRF_REJECTED"); const session = await requireSession(request); const input = dashboardCreateSchema.parse(await request.json()); const result = await createDashboard(input, session.user); await writeAudit({ actor: session.user, action: "DASHBOARD_CREATED", category: "DASHBOARD", targetType: "DASHBOARD", targetId: result.dashboardId, targetName: input.name, newValues: { ...input, businessQuestions: input.businessQuestions.length }, meta }); return Response.json(result, { status: 201 }); } catch (error) { return apiError(error, meta.requestId); } }
