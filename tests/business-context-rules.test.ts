import { describe, expect, it } from "vitest"; import { compareVersionSnapshots, detectGrainConflict, detectMeasureDuplicationRisk, nextModelStatus, validateBusinessFieldConfiguration } from "../lib/business-context/rules";
describe("Business Context rules",()=>{it("validates business types, field roles, aggregation, sensitivity and visibility",()=>{expect(validateBusinessFieldConfiguration({businessType:"TEXT",fieldRole:"MEASURE",aggregationRule:"SUM",sensitivityClassification:"PERSONAL_DATA",aiUsageAllowed:true,visibleToDashboardCreator:true}).map((item)=>item.code)).toEqual(expect.arrayContaining(["INVALID_AGGREGATION","SENSITIVE_AI"]));expect(validateBusinessFieldConfiguration({businessType:"NUMBER",fieldRole:"MEASURE",aggregationRule:"SUM",sensitivityClassification:"NONE",aiUsageAllowed:true,visibleToDashboardCreator:true})).toEqual([]);});it("detects grain and measure duplication risks",()=>{const rel={id:"r1",sourceObjectId:"a",targetObjectId:"b",cardinality:"ONE_TO_MANY"};expect(detectGrainConflict([{id:"a",recordGrain:"One order"},{id:"b",recordGrain:null}],[rel])).toHaveLength(1);expect(detectMeasureDuplicationRisk([rel])).toEqual(["r1"]);});it("enforces review and publish state transitions",()=>{expect(nextModelStatus("DRAFT","SUBMIT")).toBe("READY_FOR_REVIEW");expect(nextModelStatus("DRAFT","PUBLISH")).toBeNull();expect(nextModelStatus("PUBLISHED","CREATE_VERSION")).toBe("DRAFT");});it("compares immutable version snapshots",()=>{expect(compareVersionSnapshots({objects:[{id:"a",version:1},{id:"b",version:1}]},{objects:[{id:"a",version:2},{id:"c",version:1}]}).objects).toEqual({added:["c"],removed:["b"],modified:["a"]});});});
