import { describe, expect, it } from "vitest";
import { collectFormulaFieldIds, formulaNodeSchema, validateFormulaTypes, type FormulaNode } from "../lib/business-context/formula";
const fieldA="11111111-1111-4111-8111-111111111111";const fieldB="22222222-2222-4222-8222-222222222222";
describe("KPI formula AST",()=>{it("parses nested aggregate and ratio formulas",()=>{const node={type:"percentage",numerator:{type:"aggregate",function:"SUM",expression:{type:"field",businessFieldId:fieldA}},denominator:{type:"aggregate",function:"COUNT_DISTINCT",expression:{type:"field",businessFieldId:fieldB}}};expect(formulaNodeSchema.parse(node)).toEqual(node);expect([...collectFormulaFieldIds(node as FormulaNode)]).toEqual([fieldA,fieldB]);});it("rejects raw SQL and untyped nodes",()=>expect(formulaNodeSchema.safeParse({type:"sql",sql:"DROP TABLE X"}).success).toBe(false));it("rejects SUM of an identifier",()=>{const formula:FormulaNode={type:"aggregate",function:"SUM",expression:{type:"field",businessFieldId:fieldA}};expect(validateFormulaTypes(formula,new Map([[fieldA,{businessType:"IDENTIFIER",fieldRole:"IDENTIFIER",approvalStatus:"APPROVED"}]]))).toContain("SUM is invalid for IDENTIFIER");});});
