"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
export default function CreateUserForm() { const router = useRouter(); const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const response = await fetch("/api/admin/users", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ fullName: data.get("fullName"), username: data.get("username"), email: data.get("email"), role: data.get("role"), status: data.get("status"), password: data.get("password"), mustChangePassword: data.get("mustChangePassword") === "on", adminNotes: data.get("adminNotes") || undefined }) }); const body = await response.json(); if (!response.ok) return setError(body.error); router.push(`/admin/users/${body.user.id}`); router.refresh(); } return <form className="auth-form wide-form" onSubmit={submit}><div className="form-grid"><label>Full name<input name="fullName" required /></label><label>Username<input name="username" required minLength={3} /></label><label>Email<input name="email" required type="email" /></label><label>Role<select name="role"><option value="VIEWER">Viewer</option><option value="DASHBOARD_CREATOR">Dashboard Creator</option><option value="DATA_SOURCE_CREATOR">Data Source Creator</option><option value="ADMIN">Administrator</option></select></label><label>Status<select name="status"><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></label><label>Initial password<input name="password" type="password" minLength={10} required /></label></div><label>Admin notes<textarea name="adminNotes" rows={3} /></label><label className="check"><input name="mustChangePassword" type="checkbox" defaultChecked /> Force password change on next login</label>{error && <p className="form-error">{error}</p>}<div className="profile-actions"><button className="primary-button">Create user</button><button type="button" className="secondary-button" onClick={() => router.back()}>Cancel</button></div></form>; }
